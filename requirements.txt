# dynhosts 依存パッケージ

# YAML 設定ファイルの読み込み（必須）
pyyaml>=6.0

# システムトレイアイコン（トレイモードで必須）
pystray>=0.19.4

# トレイアイコン画像生成（トレイモードで必須）
Pillow>=10.0.0

# カスタム DNS サーバー指定時のみ必要（オプション）
# dns_server を空にする場合は不要
dnspython>=2.4.0
